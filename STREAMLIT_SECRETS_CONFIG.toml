# Configuration pour Streamlit Cloud → Settings → Secrets
# Copiez-collez ce contenu EXACTEMENT dans l'éditeur de secrets

# OPTION 1 : Connection String avec pooling (À TESTER EN PREMIER)
# Utilisez cette option si les paramètres individuels ne fonctionnent pas
[database]
connection_string = "postgresql://postgres.yrdhzpckptziyiefshga:Douane20256@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# OPTION 2 : Paramètres individuels avec PORT DE POOLING (RECOMMANDÉ)
# Utilisez le port 6543 (pooling) au lieu de 5432 pour éviter les problèmes IPv6
# NOTE: Pour le pooling, essayez d'abord avec user = "postgres" (sans projet ID)
# Si ça ne fonctionne pas, essayez avec user = "postgres.yrdhzpckptziyiefshga"
[database]
host = "aws-0-eu-central-1.pooler.supabase.com"
port = 6543
user = "postgres"
password = "Douane20256"
database = "postgres"

# IMPORTANT : 
# - La connection_string doit être sur UNE SEULE LIGNE
# - Le caractère # dans le mot de passe doit être encodé en %23 dans l'URL
# - L'option 2 (paramètres individuels) est plus sûre et évite les problèmes d'encodage
# - Si vous obtenez "Tenant or user not found":
#   1. Vérifiez le hostname de pooling dans Supabase → Settings → Database
#   2. Vérifiez le mot de passe exact dans Supabase (icône 👁️ pour voir)
#   3. Essayez d'abord user = "postgres", puis user = "postgres.PROJECT_ID" si nécessaire
